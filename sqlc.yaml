---
version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "src/assets/migrations"
    gen:
      go:
        package: "db"
        out: "src/db"
        sql_package: "pgx/v5"
        emit_interface: true
